# Cookbook 🍲

A personal cookbook built with Markdown and published via GitHub Pages.

## Structure
- Each recipe lives in its own Markdown file
- Recipes are organized by category
- The site is generated using MkDocs

## Local preview
```bash
pip install mkdocs
mkdocs serve
